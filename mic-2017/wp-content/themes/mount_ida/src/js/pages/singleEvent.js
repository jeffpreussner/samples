mic.pages.singleEvent = {
  init: function(){
    $('.event-wrapper ul, .event-wrapper ol').each(function(){
      $(this).prev().addClass('reducepadding');
    });
    if($('.wFormContainer form').length > 0){
      
    }
  }

}
